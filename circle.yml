machine:
  node:
    version: 6.1.0

test:
  post:
    - COVERALLS_SERVICE_JOB_ID=$CIRCLE_BUILD_NUM node_modules/.bin/coveralls.js < coverage/lcov.info
    - node_modules/.bin/codeclimate-test-reporter < coverage/lcov.info
    - cp -r coverage $CIRCLE_ARTIFACTS/coverage
